/**
 * Locale-aware number/date formatting (legacy module name).
 * Prefer useFormatLocale() in UI for live language switches.
 */

export {
  formatNumberFr,
  formatKg,
  formatHa,
  formatPercent,
  formatDateLocale,
  formatCalendarDateLocale,
  useFormatLocale,
  formatNumberLocale,
  formatKgLocale,
  formatHaLocale,
  formatPercentLocale,
} from './formatLocale';
